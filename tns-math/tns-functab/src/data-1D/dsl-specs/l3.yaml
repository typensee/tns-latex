# If necessary, the following specifications can be completed via
# a TeX file that will be added in the specifications document.

# We must use long name with the following regex-like wildcard.
#
#    --> no wildcard is for a mandatory context.
#    --> "?" is for a context that can be used at most one time.
#    --> "+" is for a context that must be used at least one time.
#    --> "*" is for an context that can be used as many time as
#        necessary.
#
# The order used in this file must be the same in the user's code.

- xvals: "valeurs pivots dont on veut les images"
- imgs+: "les images pour une fonction d'une variable réelle"
