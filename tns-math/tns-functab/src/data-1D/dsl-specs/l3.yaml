# The following specifications can be completed via a TeX file
# ''desc.tex'' to be added to the final specifications document.

- meta:
  title: "Images de valeurs par plusieurs fonctions à une variable"
  desc : "Voici les contextes disponibles pour les images de plusieurs fonctions à une variable."

# The technical specifications must give long name, and can use
# the following regex-like wildcard.
#
#    --> no wildcard is for a mandatory context.
#    --> "?" is for a context that can be used at most one time.
#    --> "+" is for a context that must be used at least one time.
#    --> "*" is for an context that can be used as many time as
#        necessary.
#
# The order used in this file must be the same in the user's code.

- xvals: "ce sont les valeurs pivots dont on veut les images."
- imgs+: "ce sont les images par une fonction d'une variable réelle."
