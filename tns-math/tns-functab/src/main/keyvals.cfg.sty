% == TOOLS == %

\ExplSyntaxOn

% -- KEY-VAL SETTING -- %

% :: functable ENV :: %

%%%
% prototype::
%     :see: l3func.tns_functab_functable:nn
%%%
\keys_define:nn { tns-functab / functable } {
  bar .code:n = {
    \keys_set:nn { tns-functab / functable / bar } { #1 }
  },
  bar / tmpa .dim_set:N = \l_tmpa_dim,
  bar / tmpa .initial:n = 1em,
}

\ExplSyntaxOff
